# Decision journal — 32


## Decision
BLOCKER: The `Unsupported` field does not exist on `domain.CertificationRecord` or any other domain type. The issue's non-goal explicitly says "Do not modify domain types" — yet the issue description says "skip records where Unsupported == true". The parent issue #25 was supposed to add this field (per engagement history: "Grade and CertificationRecord need N/A and Unsupported additions"). The implementer must either (a) add `Unsupported bool` to `CertificationRecord` (contravening the non-goal), (b) wait for #25 to land first, or (c) detect unsupported records via an alternative mechanism (e.g., a new `StatusUnsupported` or checking whether the language has no registered analyzer). Currently there is no programmatic way to distinguish unsupported-language records from normal ones — all records are treated identically regardless of language support.

## Decision
The `Unsupported bool` field does NOT yet exist on `domain.CertificationRecord`. The approved plan (comment on #32) says step 1 is to add it. This is required before any report consumer can filter on it. Default `false` ensures backward compatibility — all existing records and tests continue to work unchanged. The record store's `recordJSON` struct and `toJSON`/`fromJSON` functions do NOT include `Unsupported`; this is acceptable since serialization is not part of this issue's scope (the field defaults to false on deserialization).

## Decision
The unsupported-record detection mechanism is `analysis.ForLanguage(record.UnitID.Language()) == nil`, NOT a struct field `r.Unsupported`. This was established by closeout #27 and is consistent with the non-goal "Do not modify domain types." The `CertificationRecord` struct has no `Unsupported` field and non-goals prohibit adding one. The existing `partitionByLanguage()` helper in card.go uses this same mechanism but is unused in production code — it only exists and is tested in isolation.

## Decision
The changes are confined to four target files at specific aggregation loops — no new report formats, no domain-type changes, no scorer/pipeline changes:

1. **detailed.go:82-96** (`computeDimensionAverages`) — guard loop with unsupported check
2. **detailed.go:98-115** (`computeLanguageBreakdowns`) — guard loop with unsupported check (mentioned in plan comment step 2)
3. **full.go:153-190** (`buildLanguageDetail`) — guard loop with unsupported check; this function is shared by both `GenerateFullReport()` and `GenerateCard()`
4. **health.go:37-76** (`Health()`) — guard loop + replace `len(records)` denominator with counted total
5. **card.go:56-95** (`GenerateCard()`) — guard loop + replace `len(records)` denominator with counted total

Three files (detailed.go, full.go, health.go) need `"github.com/iksnae/code-certification/internal/analysis"` added to imports. card.go already imports it.

## Decision
The guard condition for skipping unsupported records is `analysis.ForLanguage(r.UnitID.Language()) == nil`. There is NO `Unsupported` bool field on `domain.CertificationRecord` — the earlier plan to add one (#36) was superseded. The `card.go` file already imports `analysis` and has a `partitionByLanguage` helper using this exact guard. `detailed.go`, `health.go`, and `full.go` each need the `analysis` import added.

## Decision
Guard mechanism: Use `analysis.ForLanguage(r.UnitID.Language()) == nil` to detect unsupported records. Although the issue body mentions an `Unsupported` field, the CertificationRecord struct does NOT currently have an `Unsupported bool` field (closeout #36 claims it was added via PR #37 but it's not present in the current code). The latest approved plan (plan-approved-ea8db984f7bb) explicitly switches to the `analysis.ForLanguage()` approach. This is also the safer choice — it's a live check against the analyzer registry, not a stored field that could be stale.

## Decision
Use `analysis.ForLanguage(r.UnitID.Language()) == nil` as the guard in all four report consumers (card.go, health.go, detailed.go, full.go), with a counted variable for denominators in Health() and GenerateCard(). The `partitionByLanguage()` helper already in card.go is not used inline — the plan calls for direct guards in each loop. No changes to domain types (the `Unsupported` field mentioned in the issue body was never added to CertificationRecord; the analysis.ForLanguage gate is the established mechanism).