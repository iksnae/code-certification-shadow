# Decision journal — 28


## Decision
Investigation complete. Five categories of test regressions identified across the codebase stemming from the parent issue #21 (Swift repos get fabricated grades). The core mechanism: Swift files are discovered by GenericScanner (.swift→swift) but have NO registered analysis.Analyzer (only go/ts/js/py/rs are registered via init()). The certifier's collectStructuralEvidence returns early for Swift (no analyzer, not Go), producing only file-level metrics evidence. This yields sparse dimension scores and a ~0.80 "hollow" grade. The fix from #21 is to recognize unsupported languages explicitly and exclude them from normal grading. The regressions to fix here are tests that assume all languages produce full dimension sets or non-N/A grades.

## Decision
DECOMPOSE FINDINGS: The code changes from issue #25 (language-tier split) have NOT been applied yet. The current state still has swift as a fully-supported language. Issue #28 must both apply the code changes AND update tests. The following concrete changes are needed across 6 change areas:

1. **cmd/certify/init_cmd.go** (lines 306-307, 704-729): Remove `swiftStandardPolicy` constant and `case "swift"` from `languagePolicy()`.
2. **testdata/policies/swift-standard.yml**: Delete this file.
3. **cmd/certify/cli_test.go** (line 93): Remove `"swift"` from `TestLanguagePolicy_AllSupported` iteration.
4. **internal/config/policy_test.go** (lines 97, 99): Change pack count from 7 to 6, remove `"swift-standard"` from expected names.
5. **internal/engine/certifier.go** (CertifyUnit in pipeline.go): Guard against unsupported languages — when `analysis.ForLanguage(lang)` returns nil AND the language is not a supported tier, emit N/A grade (GradeF with score 0, StatusDecertified or a new unsupported status).
6. **internal/report/card.go** (GenerateCard): Add unsupported-language banner when records contain N/A-grade units.
7. **internal/report/full.go** (buildLanguageDetail): Exclude unsupported-language records from aggregate score calculations.
8. **internal/engine/language_tiers_test.go**: Create — test ForLanguage returns nil for swift, non-nil for go/ts/js/py/rs; test certifier yields N/A for swift units.
9. **internal/report/card_test.go**: Add N/A-grade and unsupported-banner test cases.
10. **internal/report/full_test.go**: Add test verifying unsupported languages excluded from aggregates.

## Decision
This issue (#28) was split from #21 by operator directive after two blocked review attempts. The operator instructed: first sub-issue: remove swiftStandardPolicy + case "swift" from init_cmd.go, delete swift-standard.yml, update cli_test.go to drop "swift". The current codebase still has ALL Swift artifacts intact (swiftStandardPolicy constant, case "swift" in languagePolicy(), swift-standard.yml in testdata/policies, "swift" in cli_test.go's TestLanguagePolicy_AllSupported list, and policy_test.go expecting 7 packs with "swift-standard"). The N/A grade, unsupported status, unsupported banner, and aggregate exclusion from #27's plan are NOT implemented. The language_tiers_test.go does NOT exist. The build will likely compile cleanly, but tests that reference Swift will fail once the Swift artifacts are removed (or if they've been pre-emptively updated to not reference Swift). The DECOMPOSE phase must identify exactly which tests break and which code changes are needed, then hand off to IMPLEMENT for the changes.