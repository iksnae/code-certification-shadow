# Decision journal — 25


## Decision
The `language_tiers` package does not exist and must be created at `internal/language_tiers/`. It will define: a `Tier` type (int enum) with constants `TierCapable` (has a registered parser/analyzer — go, ts, js, py, rs), `TierGeneric` (in certifiableExts but no analyzer — swift, java, ruby, etc.), and `TierNone` (not recognized). A public function `TierForLanguage(lang string) Tier` will consult `analysis.ForLanguage` to determine capability. The package will be imported by scorer.go.

## Decision
The `Score()` function signature must change to accept language info. Simplest: add a `language string` parameter: `func Score(ev []domain.Evidence, evalResult policy.EvaluationResult, language string) domain.DimensionScores`. The caller in `CertifyUnit()` (pipeline.go:25) will pass `unit.ID.Language()`. All test callers must be updated. Alternatively, `scoreFromMetrics` could receive tier info via a separate parameter, but the pre-scoring gate in `Score()` also needs the tier — so change Score's signature.

## Decision
For the "N/A" grade and Unsupported marker: domain needs two additions — (a) a `GradeNA` constant in the Grade enum that stringifies to "N/A", and (b) an `Unsupported bool` field on `CertificationRecord`. `GradeFromScore` won't produce N/A — it's set explicitly by the short-circuit path. The `gradeEmoji` function in report/card.go already returns "⚪" for unknown grades (default case), which suits N/A.

## Decision
For the violation `else` branch that injects unmeasured dimensions at `0.80-penalty`: remove the `else` clause entirely. Only penalize dimensions already in the map. A violation for an absent dimension simply produces no effect — matching the doc contract "only dimensions with actual evidence are included." The `if _, ok := scores[v.Dimension]; ok` branch stays; the `else` branch is deleted.

## Decision
For `scoreFromMetrics`, guard the complexity-based maintainability rule: after getting `complexity`, if `complexity == 0` AND `TierForLanguage(lang)` is TierGeneric or TierNone (i.e., `analysis.ForLanguage(lang) == nil`), skip setting maintainability entirely — do not call `setMax(scores, DimMaintainability, 0.95)`. The language/tier must be threaded into `scoreFromMetrics`; simplest is to pass it as a parameter since `Score()` now receives language.

## Decision
In `CertifyUnit()` (pipeline.go), after the short-circuit returns an Unsupported record, skip the normal `WeightedAverage`, `StatusFromScore`, and `GradeFromScore` flows — set Grade=N/A, Score=0, Status=StatusExempt (or a new StatusUnsupported), and Unsupported=true directly. Also ensure the aggregate loop in `GenerateCard`/`buildLanguageDetail` skips Unsupported units. This is in report/card.go and report/full.go.

## Decision
FACT FINDINGS for iss#25:

1. NO language_tiers package exists — must be created from scratch. No TierForLanguage, TierGeneric, TierNone, TierBasic, TierFull symbols exist anywhere.

2. Score() signature is `func Score(ev []domain.Evidence, evalResult policy.EvaluationResult) domain.DimensionScores` — no language/unit parameter. Called from CertifyUnit in pipeline.go:28. CertifyUnit has the `unit domain.Unit` with `unit.ID.Language()`.

3. Grade type (domain/dimension.go) has A-F only — no N/A. GradeFromScore maps floats. Card in report/card.go already uses string "N/A" for empty records at card.go:63. gradeEmoji handles unknown grades with "⚪".

4. CertificationRecord (domain/record.go) has no Unsupported bool field.

5. 0.5 defaults injected in Score() violation handling (scorer.go lines 65-74): the else branch sets `scores[v.Dimension] = max(0, 0.80-penalty)`, which for error severity (0.30 penalty) = 0.50. This introduces correctness=0.5 and testability=0.5 for "missing evidence" violations on unsupported languages.

6. scoreFromMetrics complexity rule: `if complexity >= 0` treats 0 as "measured and simple" → sets maintainability=0.95. For Swift/unsupported languages, complexity is always 0 because no parser collects it — this is "unmeasured" not "simple."

7. Language tiers — analysis.ForLanguage(lang) returns non-nil for go/ts/py/rs (these have parsers); nil for everything else including swift, js, rb, java, kt, sh, c, cpp, cs, php, elixir, scala, lua, r, zig, sql, proto. The discovery Scanners() map also covers only go/ts/py/rs; everything else falls to GenericScanner (file-level units only).

8. Existing tests in scorer_test.go call engine.Score(ev, evalResult) directly with no language. pipeline_test.go tests CertifyUnit with go language. Card tests already handle "N/A" for empty records.

9. The Doc comment on Score() says "Only dimensions with actual evidence are included" and "Dimensions without evidence are absent" — the violation-handling else branch contradicts this, injecting dimensions at 0.80-penalty for missing-evidence violations.