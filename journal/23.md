# Decision journal — 23


## Decision
Language-tier capability audit complete. 22 unique language IDs from certifiableExts in internal/discovery/generic.go.

5 languages have FULL tier (parser + lint + test collectors):
- go: GoAnalyzer (go/ast), go vet + golangci-lint, go test
- ts: TSAnalyzer (tree-sitter), ESLint, Jest/Vitest
- js: TSAnalyzer (same tree-sitter grammar as ts), ESLint, Jest/Vitest
- py: PythonAnalyzer (tree-sitter), Ruff, pytest
- rs: RustAnalyzer (tree-sitter), cargo clippy, cargo test

17 languages are GENERIC tier (no parser, no lint, no test — only git history + line counts via GenericScanner):
rb, java, kt, sh, c, cpp, cs, swift, php, elixir, erlang, scala, lua, r, zig, sql, proto

No languages currently qualify for TierParse or TierLint (all languages with parsers also have lint+test; no language has lint-only without a parser).

TierForLanguage(lang) must return TierGeneric for unknown languages (fail-safe).

Test (a) must dynamically import discovery to get known language IDs — certifiableExts is unexported, so a minimal exported helper (e.g., KnownLanguages() or exporting CertifiableExts) is needed in discovery/generic.go for the test to iterate all IDs.

## Decision
Tier assignments based on actual collector presence audit:

**TierFull** (Parser + Lint + Test collectors all present):
- go: GoAnalyzer (analysis/go_analyzer.go:20), go vet + golangci-lint (executor.go), go test (executor.go)
- ts: TSAnalyzer (analysis/ts_analyzer.go:21), ESLint (tools.go), Jest/Vitest (tools.go)
- js: TSAnalyzer (analysis/ts_analyzer.go:22), ESLint (tools.go), Jest/Vitest (tools.go)
- py: PythonAnalyzer (analysis/python_analyzer.go:18), Ruff (tools.go), pytest (tools.go)
- rs: RustAnalyzer (analysis/rust_analyzer.go:18), cargo clippy (tools.go), cargo test (tools.go)

**TierGeneric** (git history + line counts only — no parser/lint/test collectors):
rb, java, kt, sh, c, cpp, cs, swift, php, elixir, erlang, scala, lua, r, zig, sql, proto (17 languages)

No language qualifies for TierParse-only (all 5 with parsers also have lint+test). No language qualifies for TierLint-only. TierNone is defined but unused; the registry exhaustively maps all 22 known IDs to either TierFull or TierGeneric.

## Decision
File placement: The approved plan (settled artifact "plan-approved-5a611fb72afb") puts the tier registry at internal/engine/language_tiers.go and internal/engine/language_tiers_test.go. Issue comment #14 says to extend internal/language_tiers/ instead, but that package does NOT exist — no directory, no Go files. The settled plan controls. We create internal/engine/language_tiers.go + internal/engine/language_tiers_test.go per the approved plan. Both engine package files already exist, and this fits the existing package structure.

## Decision
Tier assignments per evidence audit: Full tier (has parser + lint + test collectors): go, ts, js, py, rs (5 languages). Generic tier (only git history + line-count metrics): rb, java, kt, sh, c, cpp, cs, swift, php, elixir, erlang, scala, lua, r, zig, sql, proto (17 languages). No language qualifies for None, Lint-only, or Parse-only tiers — the only two tiers actually represented by existing collector infrastructure are Full and Generic. Registry keys are the short-code language IDs from certifiableExts (22 total).