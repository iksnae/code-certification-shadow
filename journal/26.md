# Decision journal — 26


## Decision
Production code does NOT yet implement unsupported-language handling. The `Score()` function (scorer.go:15) has no language parameter — it scores evidence generically. The `CertifyUnit` function (pipeline.go:13) has access to `unit.ID.Language()` but doesn't check for unsupported languages. `Grade` type (dimension.go:119) has no "N/A" variant. `CertificationRecord` (record.go:68) has no "Unsupported" marker. The existing tests in scorer_test.go all use Go evidence types directly. New tests must be added using either `engine.Score()` for dimension-level assertions or `engine.CertifyUnit()` for pipeline-level assertions (Grade, Status). Since no aggregate logic exists in engine package, test 5 will verify workspace-level aggregation behavior via the pipeline.

## Decision
The planned production changes from issue #25 (language_tiers.go, Grade N/A, Unsupported marker, language parameter on Score()) do NOT exist yet in the codebase. The five tests requested in issue #26 depend on those changes. Two viable approaches: (A) write tests against the existing CertifyUnit(unit, rules, ev, cfg, now) interface that takes a Unit with language, though CertifyUnit also lacks N/A/unsupported logic currently; or (B) write tests against a Score() variant with language parameter that will be added in #25, causing them to fail-compile until #25 lands. The PLAN phase must decide which approach to use given the non-goal "Do NOT change production code."

## Decision
The production code does NOT have the features that issue #26 tests are meant to verify. Specifically: (1) No language_tiers.go file exists — the concept doesn't exist in the codebase. (2) domain.Grade has no N/A constant — only GradeA through GradeF. (3) domain.CertificationRecord has no Unsupported field. (4) engine.Score() does not take a language parameter — it only takes []Evidence and EvaluationResult. (5) engine.CertifyUnit() takes a Unit but does not check language tiers; it passes all evidence through Score() regardless. (6) analysis.ForLanguage("swift") returns nil — that is how "unsupported" is currently determined, but nothing in the scoring pipeline uses this. The five tests described in the issue require production changes from issue #25 which is "not yet delivered" per the brain. Tests must be written to pass with CURRENT code per verification command.