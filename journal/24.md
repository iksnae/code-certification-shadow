# Decision journal — 24


## Decision
PLANNING: Remove the dead SwiftLint lint-clean rule (lines 709-716) from swiftStandardPolicy in cmd/certify/init_cmd.go. Choice: remove entirely (not comment-wrap), because: (1) no SwiftLint collector exists anywhere in the codebase (confirmed via exhaustive search for "SwiftLint", "swiftlint", "swift" in lint.go); (2) language_tiers.go does not exist yet — the comment approach would reference a non-existent file; (3) the rule is actively harmful — it causes fabricated grades for Swift repos (issue #21). The parallel testdata fixture (testdata/policies/swift-standard.yml:10-16) also contains the same dead rule but is NOT in scope per the issue's explicit target-files constraint; it loads without parsing individual rules so tests won't fail.

## Decision
DECOMPOSE phase — Issue #24 (Remove dead SwiftLint rule from init template):

1. TARGET BLOCK: Lines 709–716 of cmd/certify/init_cmd.go — 7 lines of YAML for the `lint-clean` rule (id through threshold: 0) plus 1 trailing blank line. This is the ONLY reference to "SwiftLint" in any .go file (confirmed via full-text search).

2. Surrounding constant: swiftStandardPolicy (lines 704–738), used only via languagePolicy("swift") at line 307. After removal, swiftStandardPolicy retains 3 rules: test-pass, no-todos, max-complexity.

3. TEST IMPACT: No tests reference the content of swiftStandardPolicy. cli_test.go:93 checks only that languagePolicy("swift") returns non-empty. Test fixtures using "lint-clean" as a rule ID (evaluator_test.go, scorer_test.go, pipeline_test.go, certifier_test.go, integration_test.go) are NOT Swift-specific — they construct mock rules in Go/test contexts. They will NOT break.

4. SEPARATE FILE: testdata/policies/swift-standard.yml also contains the lint-clean rule but is explicitly OUT OF SCOPE per the issue's target-files directive. That YAML fixture is used by policy_test.go which loads packs by name only; it does not validate rule count for swift-standard.

5. VERIFICATION: `go build ./cmd/certify/` and `go test ./... -count=1` should both pass cleanly after the removal.