# Decision journal — 31


## Decision
CRITICAL DEPENDENCY GAP: The language_tiers package (with TierForLanguage, TierGeneric) and domain types (GradeNA, CertificationRecord.Unsupported) do NOT exist in the repository. The issue description labels them "prior deliverables" but they are absent. These must be created before or as part of this work item. The analysis.ForLanguage() function exists and returns nil for unsupported languages, but there is no tier concept. The expected API inferred from the issue: language_tiers.TierForLanguage(lang string) returns a Tier type; TierGeneric is the tier for languages with no parser/analyzer (e.g., swift). The implementer must create internal/language_tiers/tiers.go and add GradeNA and Unsupported to domain, despite the non-goal note."

## Decision
DECOMPOSE finding: The language_tiers package, GradeNA constant, and Unsupported field are listed as "prior deliverables" in the issue's non-goals but do NOT exist in the repository. The plan comment in the issue (#31 comment 1) explicitly lists creating them as steps 1-3. The next phase must create these prerequisites before modifying the engine package, or the engine changes will fail to compile. The plan's steps 1-3 are authoritative (they were approved) and override the issue body's non-goals note.

## Decision
CRITICAL: The issue states "Do not create the language_tiers package or domain types (those are prior deliverables)" — but these prerequisites do NOT exist in the repository. Specifically: (1) internal/language_tiers/ directory does not exist; no Tier type, TierForLanguage(), TierGeneric, or TierNative symbols found anywhere. (2) domain.GradeNA constant does not exist in dimension.go — Grade type has A/A-/B+/B/C/D/F only. (3) domain.CertificationRecord.Unsupported bool field does not exist in record.go. These must be created BEFORE the scorer/pipeline changes can compile. The issue's "non-goal" instruction is aspirational — it assumes prior issues #29/#30/#36 delivered these foundations, but they were never merged. The next phase (architect/plan/implement) MUST create these prerequisites as the first slice(s) of work, contrary to the literal non-goal text, or the build will fail to compile. This matches the operator's own diagnosis in the issue comments: "the build re-planned already-shipped foundations... as slices 1-2, exhausting the round budget before the scorer work."

## Decision
Closeout #31 carry-forward:
- Audit remaining scoring rules for similar gate-pattern adoption where inputs may be undefined or unvalidated.
- Document the language-tier gate contract so future rule authors consistently check before scoring.
- Consider CI enforcement that no magic-number defaults re-enter the scorer configuration.