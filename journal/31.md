# Decision journal — 31


## Decision
CRITICAL DEPENDENCY GAP: The language_tiers package (with TierForLanguage, TierGeneric) and domain types (GradeNA, CertificationRecord.Unsupported) do NOT exist in the repository. The issue description labels them "prior deliverables" but they are absent. These must be created before or as part of this work item. The analysis.ForLanguage() function exists and returns nil for unsupported languages, but there is no tier concept. The expected API inferred from the issue: language_tiers.TierForLanguage(lang string) returns a Tier type; TierGeneric is the tier for languages with no parser/analyzer (e.g., swift). The implementer must create internal/language_tiers/tiers.go and add GradeNA and Unsupported to domain, despite the non-goal note."

## Decision
DECOMPOSE finding: The language_tiers package, GradeNA constant, and Unsupported field are listed as "prior deliverables" in the issue's non-goals but do NOT exist in the repository. The plan comment in the issue (#31 comment 1) explicitly lists creating them as steps 1-3. The next phase must create these prerequisites before modifying the engine package, or the engine changes will fail to compile. The plan's steps 1-3 are authoritative (they were approved) and override the issue body's non-goals note.