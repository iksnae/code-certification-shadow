From 5330a0e423a4d85296d13fcb4af10b838685b94e Mon Sep 17 00:00:00 2001
From: AgencyX <agencyx@loswf.com>
Date: Thu, 23 Jul 2026 17:13:26 -0400
Subject: [PATCH] Build: implement approved plan (sweep-build-26#s0)

---
 internal/engine/scorer_test.go | 73 ++++++++++++++++++++++++++++++++++
 1 file changed, 73 insertions(+)

diff --git a/internal/engine/scorer_test.go b/internal/engine/scorer_test.go
index 245b46eaa..40fef4d8a 100644
--- a/internal/engine/scorer_test.go
+++ b/internal/engine/scorer_test.go
@@ -2,6 +2,7 @@ package engine_test
 
 import (
 	"testing"
+	"time"
 
 	"github.com/iksnae/code-certification/internal/domain"
 	"github.com/iksnae/code-certification/internal/engine"
@@ -579,3 +580,75 @@ func TestScorer_DeepAnalysis_AllClean(t *testing.T) {
 		}
 	}
 }
+
+func TestScore_SupportedLanguage_StillWorks(t *testing.T) {
+	// Construct a Go Unit with realistic CodeMetrics evidence and passing policy
+	unit := domain.NewUnit(domain.NewUnitID("go", "main.go", "CalculateSum"), domain.UnitTypeFunction)
+	ev := []domain.Evidence{
+		evidence.CodeMetrics{
+			TotalLines:   150,
+			CodeLines:    120,
+			CommentLines: 20,
+			BlankLines:   10,
+			Complexity:   5,
+			TodoCount:    0,
+		}.ToEvidence(),
+		evidence.TestResult{
+			Tool:        "go test",
+			TotalCount:  20,
+			PassedCount: 20,
+			FailedCount: 0,
+			Coverage:    0.85,
+		}.ToEvidence(),
+		evidence.LintResult{
+			Tool:       "golangci-lint",
+			ErrorCount: 0,
+		}.ToEvidence(),
+	}
+
+	cfg := domain.ExpiryConfig{
+		DefaultWindowDays: 90,
+		MinWindowDays:     7,
+		MaxWindowDays:     365,
+	}
+
+	record := engine.CertifyUnit(unit, nil, ev, cfg, time.Now())
+
+	// Assert Grade is one of A/B/C/D/F (not N/A, not empty)
+	validGrades := []domain.Grade{domain.GradeA, domain.GradeB, domain.GradeC, domain.GradeD, domain.GradeF}
+	gradeValid := false
+	for _, g := range validGrades {
+		if record.Grade == g {
+			gradeValid = true
+			break
+		}
+	}
+	if !gradeValid {
+		t.Errorf("Grade = %v, want one of A/B/C/D/F", record.Grade)
+	}
+
+	// Assert Dimensions is non-nil with entries for maintainability, test-quality, etc.
+	if record.Dimensions == nil {
+		t.Fatal("Dimensions should not be nil")
+	}
+	if len(record.Dimensions) == 0 {
+		t.Error("Dimensions should have entries")
+	}
+
+	// Check for expected dimensions
+	expectedDims := []domain.Dimension{
+		domain.DimMaintainability,
+		domain.DimTestability,
+		domain.DimCorrectness,
+	}
+	for _, dim := range expectedDims {
+		if _, ok := record.Dimensions[dim]; !ok {
+			t.Errorf("Expected dimension %s to be present in record.Dimensions", dim)
+		}
+	}
+
+	// Assert Unsupported is false
+	if record.Unsupported {
+		t.Error("Unsupported should be false for a supported language")
+	}
+}
-- 
2.54.0 (Apple Git-156)

From 72e4b1d532f748fd609e45942db72854ea8981d9 Mon Sep 17 00:00:00 2001
From: AgencyX <agencyx@loswf.com>
Date: Thu, 23 Jul 2026 17:15:34 -0400
Subject: [PATCH] Build: implement approved plan (sweep-build-26#s1)

---
 internal/engine/scorer_test.go      | 31 +++++++++++++++++++++++++++++
 internal/engine/scorer_test_read.md |  1 +
 2 files changed, 32 insertions(+)
 create mode 100644 internal/engine/scorer_test_read.md

diff --git a/internal/engine/scorer_test.go b/internal/engine/scorer_test.go
index 40fef4d8a..67aa5c5c3 100644
--- a/internal/engine/scorer_test.go
+++ b/internal/engine/scorer_test.go
@@ -294,6 +294,37 @@ func TestScorer_TierGenericComplexitySkipsMaintainability(t *testing.T) {
 	}
 }
 
+func TestScore_UnmeasuredComplexity_NotScored(t *testing.T) {
+	// A TierGeneric unit with zero complexity should never be scored as excellent (maintainability ≠ 0.95).
+	// This is a regression test ensuring we don't fabricate an "excellent" score for unmeasured code.
+	// For TierGeneric languages, Score() returns nil, meaning no dimensions are computed.
+	// The absence of dimensions implies no fabricated excellent score.
+	ev := []domain.Evidence{{
+		Kind:    domain.EvidenceKindMetrics,
+		Source:  "metrics",
+		Passed:  true,
+		Metrics: map[string]float64{"complexity": 0},
+	}}
+
+	// Test with Swift (TierGeneric language)
+	scores := engine.Score(ev, policy.EvaluationResult{Passed: true}, "swift")
+	if scores != nil {
+		t.Errorf("TierGeneric language should return nil scores, got %v", scores)
+	}
+	// Since scores is nil, maintainability cannot be 0.95 (the old "excellent" default)
+	// The nil return indicates no dimensions were computed, preventing fabricated scores.
+
+	// Also test with Python (another TierGeneric language)
+	scoresPy := engine.Score(ev, policy.EvaluationResult{Passed: true}, "python")
+	if scoresPy != nil {
+		t.Errorf("TierGeneric language (python) should return nil scores, got %v", scoresPy)
+		if _, ok := scoresPy[domain.DimMaintainability]; ok {
+			t.Error("TierGeneric with complexity=0 should not have maintainability dimension set to any value, especially not 0.95")
+		}
+	}
+	// If scoresPy is nil, maintainability is definitely not set to 0.95
+}
+
 func TestScorer_TierFullComplexityDoesSetMaintainability(t *testing.T) {
 	// For a TierFull language with complexity=0, maintainability should be set
 	ev := []domain.Evidence{{
diff --git a/internal/engine/scorer_test_read.md b/internal/engine/scorer_test_read.md
new file mode 100644
index 000000000..5a6a5941c
--- /dev/null
+++ b/internal/engine/scorer_test_read.md
@@ -0,0 +1 @@
+Reading scorer_test.go
\ No newline at end of file
-- 
2.54.0 (Apple Git-156)

