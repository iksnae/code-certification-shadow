From 369b94825bea77524a3a33983e378fa4c6997e5c Mon Sep 17 00:00:00 2001
From: AgencyX <agencyx@loswf.com>
Date: Wed, 15 Jul 2026 19:42:41 -0400
Subject: [PATCH] Build: implement approved plan (sweep-build-27#s0)

---
 internal/report/card.go               | 21 ++++++++
 internal/report/card_internal_test.go | 77 +++++++++++++++++++++++++++
 2 files changed, 98 insertions(+)
 create mode 100644 internal/report/card_internal_test.go

diff --git a/internal/report/card.go b/internal/report/card.go
index d726534d7..d819106e7 100644
--- a/internal/report/card.go
+++ b/internal/report/card.go
@@ -7,6 +7,7 @@ import (
 	"strings"
 	"time"
 
+	"github.com/iksnae/code-certification/internal/analysis"
 	"github.com/iksnae/code-certification/internal/domain"
 )
 
@@ -40,6 +41,13 @@ type Card struct {
 
 	// Package summaries (populated for report tree navigation)
 	Packages []PackageSummary `json:"packages,omitempty"`
+
+	// Unsupported units — count and language list for banner display.
+	UnsupportedCount     int      `json:"unsupported_count"`
+	UnsupportedLanguages []string `json:"unsupported_languages,omitempty"`
+
+	// AnalyzableUnits is the number of units with a supported language.
+	AnalyzableUnits int `json:"analyzable_units"`
 }
 
 // IssueCard describes a single unit needing attention.
@@ -296,6 +304,19 @@ func FormatCardMarkdown(c Card) string {
 
 // gradeEmoji maps grades to brand-consistent status emoji.
 // 🟢 Certified (A–B), 🟡 Observations (C), 🟠 Probationary (D), 🔴 Decertified (F), ⚪ Expired.
+// partitionByLanguage splits records into supported and unsupported slices
+// based on whether analysis.ForLanguage returns a non-nil Analyzer.
+func partitionByLanguage(records []domain.CertificationRecord) (supported, unsupported []domain.CertificationRecord) {
+	for _, r := range records {
+		if analysis.ForLanguage(r.UnitID.Language()) != nil {
+			supported = append(supported, r)
+		} else {
+			unsupported = append(unsupported, r)
+		}
+	}
+	retu
...[elided 420 bytes - full stream is hash-only]...
kePartitionRecord(lang, path, symbol string) domain.CertificationRecord {
+	return domain.CertificationRecord{
+		UnitID:   domain.NewUnitID(lang, path, symbol),
+		UnitType: domain.UnitTypeFunction,
+		UnitPath: path,
+	}
+}
+
+func TestPartitionByLanguage_AllSupported(t *testing.T) {
+	records := []domain.CertificationRecord{
+		makePartitionRecord("go", "a.go", "A"),
+		makePartitionRecord("go", "b.go", "B"),
+		makePartitionRecord("ts", "c.ts", "C"),
+	}
+
+	supported, unsupported := partitionByLanguage(records)
+
+	if len(supported) != 3 {
+		t.Errorf("supported = %d, want 3", len(supported))
+	}
+	if len(unsupported) != 0 {
+		t.Errorf("unsupported = %d, want 0", len(unsupported))
+	}
+}
+
+func TestPartitionByLanguage_AllUnsupported(t *testing.T) {
+	records := []domain.CertificationRecord{
+		makePartitionRecord("unknown-lang", "x.xyz", "X"),
+		makePartitionRecord("madeup", "y.abc", "Y"),
+	}
+
+	supported, unsupported := partitionByLanguage(records)
+
+	if len(supported) != 0 {
+		t.Errorf("supported = %d, want 0", len(supported))
+	}
+	if len(unsupported) != 2 {
+		t.Errorf("unsupported = %d, want 2", len(unsupported))
+	}
+}
+
+func TestPartitionByLanguage_Mixed(t *testing.T) {
+	records := []domain.CertificationRecord{
+		makePartitionRecord("go", "a.go", "A"),
+		makePartitionRecord("unknown-lang", "x.xyz", "X"),
+		makePartitionRecord("ts", "b.ts", "B"),
+		makePartitionRecord("madeup", "y.abc", "Y"),
+	}
+
+	supported, unsupported := partitionByLanguage(records)
+
+	if len(supported) != 2 {
+		t.Errorf("supported = %d, want 2", len(supported))
+	}
+	if len(unsupported) != 2 {
+		t.Errorf("unsupported = %d, want 2", len(unsupported))
+	}
+}
+
+func TestPartitionByLanguage_Empty(t *testing.T) {
+	supported, unsupported := partitionByLanguage(nil)
+
+	if len(supported) != 0 {
+		t.Errorf("supported = %d, want 0", len(supported))
+	}
+	if len(unsupported) != 0 {
+		t.Errorf("unsupported = %d, want 0", len(unsupported))
+	}
+}
-- 
2.54.0 (Apple Git-156)

