From aa2d8187d6866a3ac3efdc5416b528c3d6a14408 Mon Sep 17 00:00:00 2001
From: AgencyX <agencyx@loswf.com>
Date: Thu, 16 Jul 2026 14:49:26 -0400
Subject: [PATCH] Build: implement approved plan (sweep-build-29#s0)

---
 internal/language_tiers/language_tiers.go     | 26 +++++++++++++++
 .../language_tiers/language_tiers_test.go     | 33 +++++++++++++++++++
 2 files changed, 59 insertions(+)
 create mode 100644 internal/language_tiers/language_tiers.go
 create mode 100644 internal/language_tiers/language_tiers_test.go

diff --git a/internal/language_tiers/language_tiers.go b/internal/language_tiers/language_tiers.go
new file mode 100644
index 000000000..1797b4d63
--- /dev/null
+++ b/internal/language_tiers/language_tiers.go
@@ -0,0 +1,26 @@
+package language_tiers
+
+import "github.com/iksnae/code-certification/internal/analysis"
+
+// Tier represents the level of language support in the certification system.
+type Tier int
+
+const (
+	// TierGeneric covers languages not explicitly registered in the analysis
+	// package. These languages receive basic, generic certification support.
+	TierGeneric Tier = iota
+
+	// TierFull covers languages that are explicitly registered with a dedicated
+	// analyzer (go, ts, py, rs, js). These languages receive full certification.
+	TierFull
+)
+
+// TierForLanguage returns the certification tier for the given language
+// identifier. Languages registered in the analysis package return TierFull;
+// all others return TierGeneric.
+func TierForLanguage(lang string) Tier {
+	if analysis.ForLanguage(lang) != nil {
+		return TierFull
+	}
+	return TierGeneric
+}
diff --git a/internal/language_tiers/language_tiers_test.go b/internal/language_tiers/language_tiers_test.go
new file mode 100644
index 000000000..208968f67
--- /dev/null
+++ b/internal/language_tiers/language_tiers_test.go
@@ -0,0 +1,33 @@
+package language_tiers
+
+import "testing"
+
+func TestTierForLanguage(t *testing.T) {
+	tests := []struct {
+		name string
+		lang string
+		want Tier
+	}{
+		// Registered languages → TierFull
+		{name: "go", lang: "go", want: TierFull},
+		{name: "typescript", lang: "ts", want: TierFull},
+		{name: "python", lang: "py", want: TierFull},
+		{name: "rust", lang: "rs", want: TierFull},
+		{name: "javascript", lang: "js", want: TierFull},
+
+		// Unregistered languages → TierGeneric
+		{name: "ruby", lang: "ruby", want: TierGeneric},
+		{name: "swift", lang: "swift", want: TierGeneric},
+		{name: "c", lang: "c", want: TierGeneric},
+		{name: "empty string", lang: "", want: TierGeneric},
+	}
+
+	for _, tt := range tests {
+		t.Run(tt.name, func(t *testing.T) {
+			got := TierForLanguage(tt.lang)
+			if got != tt.want {
+				t.Errorf("TierForLanguage(%q) = %v, want %v", tt.lang, got, tt.want)
+			}
+		})
+	}
+}
-- 
2.54.0 (Apple Git-156)

