# Closeout #31 — Guard Score() with language-tier gate, protect complexity maintainability rule, remove 0.5 defaults, update pipeline, and fix all engine tests

`issue:iksnae/code-certification#31` · closed 2026-07-23T03:21:00Z

## Delivery
- Outcome: **pass** (confidence 78%)
- Delivered: PR #38 (a410f437)
- Verify: GREEN · 74 build rounds

## Files changed (4)
- internal/engine/pipeline.go
- internal/engine/pipeline_test.go
- internal/engine/scorer.go
- internal/engine/scorer_test.go

## Acceptance
_none_

## Reflection

Status: **Complete. PR #38 (a410f437) merged — 1 track delivered across 4 files.**

### Validation
_None._

### Retrospective
- Gate patterns (language-tier check before scoring) prevent invalid inputs from cascading into downstream rules, reducing silent failures.
- Removing 0.5 magic-number defaults eliminates ambiguity in scoring thresholds; explicit configuration surfaces intent.
- Pipeline updates that touch scoring, gating, and defaults simultaneously require coordinated test fixes across scorer_test.go and pipeline_test.go to avoid regressions.
- Complexity/maintainability rules need explicit opt-in protection rather than implicit assumptions — the guard pattern generalizes well beyond this single rule.

### Carry-forward
- Audit remaining scoring rules for similar gate-pattern adoption where inputs may be undefined or unvalidated.
- Document the language-tier gate contract so future rule authors consistently check before scoring.
- Consider CI enforcement that no magic-number defaults re-enter the scorer configuration.

### Validator Notes
- scorer.go and pipeline.go are tightly coupled: gating in Score() must stay synchronized with pipeline orchestration to avoid double-gating.
- Both *_test.go files were updated in lockstep with their production counterparts, preserving test coverage through the refactor.
- No new public API surface was introduced; the guard is internal to the engine package, limiting blast radius.