# Closeout #23 — Create a language-tier registry mapping every known language ID to an analysis capability tier (None/Generic/Lint/Parse/Full), with a test that fails CI when a new language extension is added to discovery without a tier entry.

`issue:iksnae/code-certification#23` · closed 2026-07-23T21:22:27Z

## Delivery
- Outcome: **pass** (confidence 80%)
- Delivered: PR #41 (a4e74229)
- Verify: GREEN · 49 build rounds

## Files changed (5)
- internal/discovery/generic.go
- internal/discovery/generic_test.go
- internal/engine/language_tiers.go
- internal/engine/language_tiers_registry_test.go
- internal/engine/language_tiers_test.go

## Acceptance
_none_

## Reflection

Status: **Complete. PR #41 merged; language-tier registry with CI-enforcement test delivered.**

### Validation
_None._

### Retrospective
- The registry-completeness test (language_tiers_registry_test.go) is the real guardrail — it ensures the process is self-healing by failing CI when a new extension enters discovery without a tier, rather than relying on humans to remember.
- Co-locating the tier definitions (engine/language_tiers.go) with the registry test made the mapping auditable in a single read; splitting it from discovery kept the discovery package focused on file-walking.

### Carry-forward
_None._

### Validator Notes
- Architecture: language_tiers.go is an engine-level dependency consumed by discovery/generic.go — the dependency direction is correct (discovery depends on engine, not the reverse).
- Schema integrity: the registry completeness test couples to discovery's extension list, so any new extension added to discovery/generic.go will trigger a test failure until a tier is assigned — the enforcement loop is closed.